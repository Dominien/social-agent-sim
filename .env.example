# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Model used for agent behavior (default: haiku)
# Options: haiku, sonnet
CHARACTER_MODEL=haiku

# Log verbosity: minimal | normal | verbose
LOG_LEVEL=normal

# Delay between ticks in ms (0 = no delay)
TICK_INTERVAL_MS=0
